const pgClient = require('pg-fikar');

let snap = new pgClient.Snap({
    isProduction : false,
    serverKey : 'YOUR_SERVER_KEY',
    clientID : 'YOUR_CLIENT_ID'
});

let parameter = {
    test:"hellow fikar"
}
let data = snap.generateToken(parameter)
console.log(data)